version: '3.8'

services:
  bot:
    build: .
    container_name: lare-bot
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
    volumes:
      - ./Lare_Logs:/app/Lare_Logs